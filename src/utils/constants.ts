export const TILE_SIZE = 16;

export enum Direction {
  None = "none",
  Left = "left",
  Up = "up",
  Right = "right",
  Down = "down",
};

export enum Mode {
  Normal = "normal",
  Command = "command",
};